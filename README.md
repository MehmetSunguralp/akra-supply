## Installation Steps

- Run `npm install`
- Create an `.env` file in root directory with the following content: `VITE_BASE_URL=http://localhost:3000`
- Run `npx json-server db.json` to serve dummy data through `json-server`
- Run `npm run dev` in another terminal for client
